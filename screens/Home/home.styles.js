import { StyleSheet } from "react-native";
const s = StyleSheet.create({
  cardContainer: {
    width: "100%",
    justifyContent: "center",
    alignItems: "center",
    gap: 8,
    position: "relative",
    marginTop: 10,
    marginBottom: 30,
  },
});

export default s;
